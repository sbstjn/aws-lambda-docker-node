{
  "name": "aws-lambda-docker-node",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "docker build . -t foo:bar"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
